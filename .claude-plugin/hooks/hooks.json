{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "LOCKFILE='bun.lock'; if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE \"$LOCKFILE\"; then echo \"Blocked: do not edit lockfiles directly. Run bun install instead.\" && exit 2; fi"
          }
        ]
      }
    ]
  }
}
