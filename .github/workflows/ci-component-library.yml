name: CI - Component Library

on:
  push:
    branches: [main]
    paths:
      - 'packages/notation/**'
      - 'packages/roller/**'
      - 'packages/display-utils/**'
      - 'packages/component-library/**'
      - '.github/workflows/ci-component-library.yml'
  pull_request:
    paths:
      - 'packages/notation/**'
      - 'packages/roller/**'
      - 'packages/display-utils/**'
      - 'packages/component-library/**'
      - '.github/workflows/ci-component-library.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: component-library
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Read Bun version
        id: bun-version
        run: echo "version=$(cat .bun-version)" >> $GITHUB_OUTPUT
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ steps.bun-version.outputs.version }}
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - run: bun run --filter '@randsum/notation' build
      - run: bun run --filter '@randsum/roller' build
      - run: bun run --filter '@randsum/display-utils' build
      - run: bun run --filter '@randsum/component-library' check
      - name: Check bundle size
        run: bun run --filter '@randsum/component-library' size
      - name: Check unused exports
        run: bun run --filter '@randsum/component-library' check:exports
