name: CI - Notation

on:
  push:
    branches: [main]
    paths:
      - 'packages/notation/**'
      - '.github/workflows/ci-notation.yml'
  pull_request:
    paths:
      - 'packages/notation/**'
      - '.github/workflows/ci-notation.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: notation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Read Bun version
        id: bun-version
        run: echo "version=$(cat .bun-version)" >> $GITHUB_OUTPUT
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ steps.bun-version.outputs.version }}
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - run: bun run --filter '@randsum/notation' check
      - name: Check bundle size
        run: bun run --filter '@randsum/notation' size
      - name: Check unused exports
        run: bun run --filter '@randsum/notation' check:exports
