name: Publish + Release
on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

env:
  NPM_CONFIG_TOKEN: ${{ secrets.NPM_TOKEN }}

jobs:
  publish:
    name: Publish to NPM
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Restore build cache
        uses: actions/cache@v4
        with:
          path: |
            .moon/cache
            apps/*/dist
            corePackages/*/dist
            gamePackages/*/dist
          key: ${{ runner.os }}-build-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-build-

      - run: bun install

      - name: Build packages
        run: bun run moon :build -u

      - name: Publish to NPM
        run: |
          bun run moon notation:publish -u
          bun run moon dice:publish -u
          bun run moon fifth:publish -u
          bun run moon root-rpg:publish -u
          bun run moon salvageunion:publish -u
          bun run moon blades:publish -u
          bun run moon daggerheart:publish -u
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
