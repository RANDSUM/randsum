$schema: 'https://moonrepo.dev/schemas/tasks.json'

tasks:
  build:
   command: 'npx bunup'

  test:
   command: 'bun test'

  lint:
   command: 'eslint .'

  format:
   command: 'prettier . --check'

  tsCheck:
   command: 'tsc --erasableSyntaxOnly'
