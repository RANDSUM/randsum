/**
 * @file Root RPG dice rolling for RANDSUM
 * @module @randsum/root-rpg
 *
 * This module provides dice rolling functions specifically designed for
 * the Root RPG system, implementing the 2d6 mechanics with success/failure
 * determination based on target numbers and modifiers.
 */

export { rollRoot } from './rollRoot'
export type { RootMiss, RootResult, RootStrongHit, RootWeakHit } from './types'

