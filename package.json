{
  "name": "randsum-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ci": "bun run build && bun run --filter='*' test && bun run --filter='*' lint && bun run --filter='*' format && bun run --filter='*' ts:check",
    "build": "bun run --filter=@randsum/core build && bun run --filter=@randsum/notation build && bun run --filter=@randsum/dice build && bun run --filter='@randsum/5e' build && bun run --filter=@randsum/blades build && bun run --filter=@randsum/root-rpg build && bun run --filter=@randsum/salvageunion build && bun run --filter=@randsum/daggerheart build",
    "test": "bun run --filter='*' test",
    "lint": "bun run --filter='*' lint",
    "lint:fix": "bun run --filter='*' lint:fix",
    "format": "bun run --filter='*' format",
    "format:fix": "bun run --filter='*' format:fix",
    "ts:check": "bun run --filter='*' ts:check",
    "test:ci": "bun run --filter='*' test --coverage"
  },
  "devDependencies": {
    "@eslint/js": "^9.27.0",
    "@types/bun": "1.2.13",
    "@typescript-eslint/eslint-plugin": "^8.32.1",
    "@typescript-eslint/parser": "^8.32.1",
    "bunup": "^0.7.6",
    "eslint": "^9.27.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.32.1"
  },
  "engines": {
    "node": "latest"
  }
}