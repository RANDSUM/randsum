{
  "name": "randsum-monorepo",
  "private": true,
  "scripts": {
    "build": "moon run :build",
    "test": "moon run :test",
    "lint": "moon run :lint",
    "format": "moon run :format",
    "clean": "moon clean",
    "ci": "moon ci",
    "run": "moon run"
  },
  "trustedDependencies": ["@moonrepo/cli"],
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@moonrepo/cli": "^1.34.0"
  }
}
