coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 5%
    patch:
      default:
        target: 70%
        threshold: 5%
  ignore:
    - "apps/cli/src/tui/hooks/useCursorPosition.ts"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true
