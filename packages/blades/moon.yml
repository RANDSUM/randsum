$schema: 'https://moonrepo.dev/schemas/project.json'

id: blades
language: 'typescript'
dependsOn:
  - 'roller'
tags:
  - 'game'

tasks:
  tsCheck:
    deps:
      - 'roller:build'

  build:
    deps:
      - 'roller:build'
  test:
    command: 'echo "No tests for Blades"'
    deps:
      - 'roller:build'
    options:
      cache: false
      mergeArgs: 'replace'
      mergeEnv: 'replace'

  lint:
    deps:
      - 'roller:build'
