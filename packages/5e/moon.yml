$schema: 'https://moonrepo.dev/schemas/project.json'

id: fifth
language: 'typescript'
dependsOn:
  - 'dice'
tags:
  - 'game'

tasks:
  tsCheck:
    deps:
  build:
    deps:
      - 'dice:build'
  test:
    deps:
      - 'dice:build'
  lint:
    deps:
      - 'dice:build'
  format:
    deps:
      - 'dice:build'
