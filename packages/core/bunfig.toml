[test]
coverage = false
coverageReporter = ['text', 'lcov']
coverageThreshold = { lines = 1.0, functions = 1.0, branches = 1.0, statements = 1.0 }
coverageDirectory = "coverage"
coverageIgnorePatterns = [
  "node_modules/",
  "dist/",
  "**/*.d.ts",
  "**/*.test.ts",
  "**/test/**"
]
