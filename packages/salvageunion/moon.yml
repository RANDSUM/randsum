$schema: 'https://moonrepo.dev/schemas/project.json'

id: salvageunion
language: 'typescript'
dependsOn:
  - 'roller'
tags:
  - 'game'

tasks:
  tsCheck:
    deps:
      - 'roller:build'
  build:
    deps:
      - 'roller:build'
  test:
    command: 'echo "No tests for Salvage Union"'
    deps:
      - 'roller:build'
    options:
      cache: false
      mergeArgs: 'replace'
      mergeEnv: 'replace'
  lint:
    deps:
      - 'roller:build'
