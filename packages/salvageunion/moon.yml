$schema: 'https://moonrepo.dev/schemas/project.json'

id: salvageunion
language: 'typescript'
dependsOn:
  - 'dice'
tags:
  - 'game'

tasks:
  tsCheck:
    deps:
      - 'dice:build'
  build:
    deps:
      - 'dice:build'
