$schema: 'https://moonrepo.dev/schemas/project.json'

id: daggerheart
language: 'typescript'
dependsOn:
  - 'roller'
tags:
  - 'game'

tasks:
  tsCheck:
    deps:
      - 'roller:build'
  build:
    deps:
      - 'roller:build'
  test:
    command: 'echo "No tests for Daggerheart"'
    deps:
      - 'roller:build'
    options:
      cache: false
      mergeArgs: 'replace'
      mergeEnv: 'replace'

  lint:
    deps:
      - 'roller:build'
