---
import { LiveRepl } from './LiveRepl'

interface Props {
  code: string
  lang?: string
  readonly?: boolean
}

const { code, lang, readonly } = Astro.props
---

<LiveRepl code={code} lang={lang} readonly={readonly} client:only="react" />
