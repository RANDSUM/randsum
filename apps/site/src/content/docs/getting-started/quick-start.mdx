---
title: Quick Start — Roll Dice with TypeScript in 5 Minutes
description: Get rolling in minutes. Learn notation strings like 4d6L, drop/keep modifiers, exploding dice, and the roll() result object with live interactive examples.
---

import CodeExample from '../../../components/live-repl/CodeExample.astro'
import { RollerPlayground } from '@randsum/component-library'

This guide walks you through the basics of using `@randsum/roller` to roll dice in your application.

## Your first roll

<CodeExample code={`import { roll } from '@randsum/roller'

// Roll a single d20
const result = roll(20)
console.log(result.total) // 1-20`} />

<RollerPlayground defaultNotation="1d20" client:only="react" />

The \`roll()\` function accepts several argument types. A plain number means "roll one die with that many sides."

## Notation strings

RANDSUM supports standard dice notation. The format is \`NdS\` where **N** is the number of dice and **S** is the number of sides.

<RollerPlayground defaultNotation="2d6" client:only="react" />
<RollerPlayground defaultNotation="4d8" client:only="react" />
<RollerPlayground defaultNotation="1d20" client:only="react" />

## Drop modifiers

Drop the lowest or highest dice from a roll:

<RollerPlayground defaultNotation="4d6L" client:only="react" />
<RollerPlayground defaultNotation="2d20L" client:only="react" />
<RollerPlayground defaultNotation="2d20H" client:only="react" />
<RollerPlayground defaultNotation="4d6L2" client:only="react" />

## Arithmetic

Add or subtract from the roll total:

<RollerPlayground defaultNotation="1d20+5" client:only="react" />

<RollerPlayground defaultNotation="2d6-1" client:only="react" />

## Reroll and explode

<RollerPlayground defaultNotation="4d6R{1}" client:only="react" />

<RollerPlayground defaultNotation="4d6R{<3}" client:only="react" />
<RollerPlayground defaultNotation="3d6!" client:only="react" />

## Combining modifiers

Chain multiple modifiers together:

<RollerPlayground defaultNotation="4d6R{1}L+2" client:only="react" />
<RollerPlayground defaultNotation="3d6!L" client:only="react" />

## Options Object

Every notation string has an equivalent options object form:

<CodeExample code={`import { roll } from '@randsum/roller'

// These are equivalent:
roll('4d6L')
roll({
  sides: 6,
  quantity: 4,
  modifiers: { drop: { lowest: 1 } }
})`} />

<RollerPlayground defaultNotation="4d6L" client:only="react" />

The options object gives you full programmatic control when notation strings are not flexible enough.

## Result Object

The \`roll()\` function returns a result object with useful properties:

<CodeExample code={`import { roll } from '@randsum/roller'

const result = roll('4d6L+2')

console.log(result.total)  // Final total after all modifiers
console.log(result.rolls)  // Array of individual roll records`} />

<RollerPlayground defaultNotation="4d6L+2" client:only="react" />

:::note
Hardcoded notation like `roll('4d6L')` will never throw in practice. `roll()` only throws when given invalid input from outside your control — use `validateNotation()` or `isDiceNotation()` before rolling user-provided strings.
:::

## Multiple Groups

Roll different dice types in a single expression:

<CodeExample code={`import { roll } from '@randsum/roller'

// Attack + damage in one call
roll('1d20+5', '2d6+3')`} />

## Next steps

- **[Dice Notation Reference](/reference/dice-notation/)** -- Full syntax for all notation features
- **[Roll Options](/reference/roll-options/)** -- Options object API reference
- **[Modifiers](/reference/modifiers/)** -- Complete modifier system documentation
- **[Game Packages](/games/overview/)** -- Use game-specific packages for D&D, Blades, PbtA, and more
