---
title: RANDSUM FAQ — Common Questions About the Dice Library
description: "Answers to common questions: when roll() throws, seeded RNG for testing, browser support, reroll infinite loop prevention, and how drop vs. keep differ."
head:
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "When does roll() throw?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "roll() throws a RandsumError when given invalid input: invalid notation strings, invalid option values (like sides: 0), or modifiers with impossible conditions. Hardcoded notation like roll('4d6L') will never throw in practice — errors only occur with input from outside your control."
            }
          },
          {
            "@type": "Question",
            "name": "How do I use RANDSUM in a browser?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "All RANDSUM packages are zero-dependency and ship as ESM, making them compatible with modern browsers, Vite, webpack, and other bundlers without any special configuration."
            }
          },
          {
            "@type": "Question",
            "name": "How do I write deterministic tests for dice rolls?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Pass a custom randomFn option to roll() to provide seeded or deterministic random values. This makes dice outcomes reproducible for unit tests."
            }
          },
          {
            "@type": "Question",
            "name": "Can reroll modifiers cause infinite loops?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "No. RANDSUM caps reroll attempts at 99 by default. You can also specify a maximum reroll count with the M suffix: R{<3}5 means reroll below 3 up to 5 times."
            }
          },
          {
            "@type": "Question",
            "name": "What is the difference between drop and keep modifiers?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Drop removes dice from the pool (4d6L drops the lowest die). Keep retains only the specified dice (4d6K3 keeps the 3 highest). They are logical complements — 4d6L and 4d6K3 produce identical results."
            }
          }
        ]
      }
---

import CodeExample from '../../../components/live-repl/CodeExample.astro'

## When does `roll()` throw?

`roll()` throws a `RandsumError` when given invalid input — invalid notation strings, bad option values (like `sides: 0`), or modifiers with impossible conditions. Hardcoded notation in your source will never throw in practice.

For user-provided input, validate before rolling:

<CodeExample code={`import { validateNotation, isDiceNotation, roll } from '@randsum/roller'

// Option 1: validate and get structured error info
const validation = validateNotation(userInput)
if (!validation.valid) {
  console.error(validation.error.message)
} else {
  const result = roll(userInput)
}

// Option 2: type guard
if (isDiceNotation(userInput)) {
  const result = roll(userInput) // typed as DiceNotation
}`} />

See [Error Handling](/guides/error-handling/) for the full pattern.

## How do I roll a 0-dice Blades pool?

Pass `0` to `roll()` from `@randsum/blades`. A 0-dice pool represents a **desperate position** in Blades in the Dark — the system rolls 2d6 and keeps the lowest die.

<CodeExample code={`import { roll } from '@randsum/blades'

const result = roll(0) // Rolls 2d6, keeps lowest
console.log(result.result) // 'critical' | 'success' | 'partial' | 'failure'`} />

## How do I access individual die results?

Every `roll()` result includes a `rolls` array of `RollRecord` objects — one per dice group. Each record contains the individual die values before and after modifiers.

<CodeExample code={`import { roll } from '@randsum/roller'

const result = roll('4d6L')

// Access all roll records
result.rolls.forEach(record => {
  console.log(record.modifierHistory.modifiedRolls)  // die values after modifiers
  console.log(record.rolls)                          // die values before modifiers
})`} />

## Can I use RANDSUM in the browser?

Yes. `@randsum/roller` has zero runtime dependencies and uses only standard JavaScript — no Node.js built-ins. It works in any environment that runs modern JS: browsers, Deno, Bun, Edge functions, and Node.js 18+.

## What runtimes are supported?

| Runtime | Supported |
|---|---|
| Node.js 18+ | Yes |
| Bun 1.0+ | Yes |
| Deno | Yes |
| Browser (ESM) | Yes |
| Edge functions (Cloudflare, Vercel) | Yes |

## How does `reroll` prevent infinite loops?

The reroll modifier has a built-in attempt limit (`MAX_REROLL_ATTEMPTS = 99`). If the reroll condition can never be satisfied (e.g., rerolling all results on a d1), RANDSUM stops after the limit and returns the last rolled value. It will never hang.

## Can I seed the random number generator?

Yes — pass a `randomFn` in the `RollConfig` argument. Any function that returns a number in `[0, 1)` works. Here is a simple seeded random function you can use:

<CodeExample code={`import { roll } from '@randsum/roller'

// Simple seeded random (linear congruential generator)
function seededRandom(seed) {
  let s = seed
  return () => {
    s = (s * 1664525 + 1013904223) >>> 0
    return s / 0x100000000
  }
}

const result = roll('4d6L', { randomFn: seededRandom(42) })
// Same result every time with seed 42
console.log(result.total)`} />

See [Testing](/guides/testing/) for more patterns.

## Do the game packages work together?

No — and by design. Game packages depend only on `@randsum/roller`. They never depend on each other. Install only the packages your application needs.

## What's the difference between `drop` and `keep`?

Both select a subset of dice — they approach it from opposite directions:

- `drop: { lowest: 1 }` — removes 1 die, keeps the rest
- `keep: { highest: 3 }` — keeps 3 dice, removes the rest

On a `4d6` roll, `4d6L` (drop lowest 1) and `keep: { highest: 3 }` produce identical results.
