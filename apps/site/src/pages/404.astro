---
import BaseLayout from '../layouts/BaseLayout.astro'
---

<BaseLayout
  title="Page Not Found"
  description="The page you're looking for doesn't exist."
>
  <div class="not-found-page">
    <div class="container">
      <div class="not-found-content">
        <div class="dice-icon">🎲</div>
        <h1>404</h1>
        <p class="subtitle">Critical Failure!</p>
        <p class="description">
          The page you're looking for rolled a natural 1 and doesn't exist.
        </p>
        
        <div class="actions">
          <a href="/" class="action-button primary">Go Home</a>
          <a href="/docs/" class="action-button secondary">View Docs</a>
        </div>

        <div class="suggestions">
          <h2>You might be looking for:</h2>
          <ul>
            <li><a href="/getting-started/">Getting Started Guide</a></li>
            <li><a href="/docs/notation/">Dice Notation Reference</a></li>
            <li><a href="/packages/roller/">@randsum/roller Package</a></li>
            <li><a href="/packages/discord-bot/">Discord Bot</a></li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .not-found-page {
    padding: var(--spacing-3xl) 0;
    min-height: calc(100vh - 300px);
    display: flex;
    align-items: center;
  }

  .not-found-content {
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }

  .dice-icon {
    font-size: 5rem;
    margin-bottom: var(--spacing-md);
    animation: roll 2s ease-in-out infinite;
  }

  @keyframes roll {
    0%, 100% {
      transform: rotate(0deg);
    }
    25% {
      transform: rotate(10deg);
    }
    75% {
      transform: rotate(-10deg);
    }
  }

  h1 {
    font-size: var(--font-size-5xl);
    font-weight: var(--font-weight-black);
    margin-bottom: var(--spacing-sm);
    line-height: 1;
  }

  .subtitle {
    font-size: var(--font-size-2xl);
    color: var(--color-primary);
    font-weight: var(--font-weight-bold);
    margin-bottom: var(--spacing-lg);
  }

  .description {
    font-size: var(--font-size-lg);
    color: var(--color-text-alt);
    margin-bottom: var(--spacing-2xl);
  }

  .actions {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: var(--spacing-3xl);
  }

  .action-button {
    display: inline-block;
    padding: var(--spacing-sm) var(--spacing-xl);
    text-decoration: none;
    font-weight: var(--font-weight-bold);
    border: var(--border-width-thick) solid var(--color-border);
    transition: all var(--transition-fast);
  }

  .action-button.primary {
    background: var(--color-text);
    color: var(--color-bg);
    border-color: var(--color-text);
  }

  .action-button.primary:hover {
    background: var(--color-bg);
    color: var(--color-text);
  }

  .action-button.secondary {
    background: var(--color-bg);
    color: var(--color-text);
  }

  .action-button.secondary:hover {
    background: var(--color-text);
    color: var(--color-bg);
  }

  .suggestions {
    text-align: left;
    padding: var(--spacing-xl);
    border: var(--border-width) solid var(--color-border);
    background: var(--color-bg);
  }

  .suggestions h2 {
    font-size: var(--font-size-lg);
    margin-bottom: var(--spacing-md);
  }

  .suggestions ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .suggestions li {
    padding: var(--spacing-xs) 0;
  }

  .suggestions a {
    color: var(--color-primary);
    text-decoration: none;
  }

  .suggestions a:hover {
    text-decoration: underline;
  }
</style>
