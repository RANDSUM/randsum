$schema: 'https://moonrepo.dev/schemas/project.json'

id: randsum-api
language: 'typescript'
dependsOn:
  - 'dice'
  - 'notation'
tags:
  - 'app'
  - 'api'

tasks:
  start:
    command: 'bun run start'

  tsCheck:
    deps:
      - 'dice:build'
      - 'notation:build'
  build:
    command: 'echo "Nothing to build for randsum-api"'
  test:
    deps:
      - 'dice:build'
      - 'notation:build'
  lint:
    deps:
      - 'dice:build'
      - 'notation:build'
  format:
    deps:
      - 'dice:build'
      - 'notation:build'
