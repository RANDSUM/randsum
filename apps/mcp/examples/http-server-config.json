{
  "mcpServers": {
    "randsum-http": {
      "command": "npx",
      "args": [
        "@randsum/mcp",
        "--transport",
        "http",
        "--port",
        "3000",
        "--host",
        "localhost"
      ],
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
