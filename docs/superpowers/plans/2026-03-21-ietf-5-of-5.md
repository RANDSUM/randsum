# RDN Spec IETF 5/5 — Final Polish Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close all remaining IETF gaps to bring every category to 5/5. All changes are to the single file `apps/rdn/src/content/specs/v1.0-alpha.md`.

**Architecture:** Six small, independent edits to the spec markdown. No code changes. Each task is 2-5 minutes.

**Tech Stack:** Markdown only. Verify with `bun run --filter '@randsum/rdn' build`.

---

## File Map

- Modify: `apps/rdn/src/content/specs/v1.0-alpha.md`

All six tasks touch this one file. They modify different sections and can be done in sequence without conflicts.

---

### Task 1: Add Authors' Addresses Section

**Files:**
- Modify: `apps/rdn/src/content/specs/v1.0-alpha.md` — add after the last appendix

- [ ] **Step 1: Add the section**

After the last appendix (Appendix G), add:

```markdown
## Authors' Addresses

Alex Jarvis
RANDSUM
Email: alxjrvs@gmail.com
URI: https://github.com/alxjrvs
```

- [ ] **Step 2: Verify build**

Run: `bun run --filter '@randsum/rdn' build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add apps/rdn/src/content/specs/v1.0-alpha.md
git commit -m "feat(spec): add Authors' Addresses section"
```

---

### Task 2: Add Document History Section

**Files:**
- Modify: `apps/rdn/src/content/specs/v1.0-alpha.md` — add after Section 1.4 (Versioning)

- [ ] **Step 1: Add document history**

After Section 1.4 Versioning, add:

```markdown
### 1.5 Document History

| Version | Date | Changes |
|---------|------|---------|
| 1.0-alpha | 2026-03-20 | Initial publication. Three-stage pipeline, 26 modifiers, four conformance levels, faceted classification. |
| 1.0-alpha rev.1 | 2026-03-21 | Added Abstract, Security Considerations (9.6), IANA Considerations (11), References (12). Added character encoding (8.0), whitespace (8.2), lexical segmentation (8.3) rules. Added Appendix F (ABNF grammar) and Appendix G (conformance test vectors). Fixed Explode single-pass contradiction, Unique ordering claim, Appendix C stage names, options type mismatches. |
```

- [ ] **Step 2: Verify build**

Run: `bun run --filter '@randsum/rdn' build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add apps/rdn/src/content/specs/v1.0-alpha.md
git commit -m "feat(spec): add Document History section (1.5)"
```

---

### Task 3: Add Annotation Privacy Note to Security Considerations

**Files:**
- Modify: `apps/rdn/src/content/specs/v1.0-alpha.md` — add to Section 9.6

- [ ] **Step 1: Add privacy paragraph**

In Section 9.6 (Security Considerations), add a new paragraph after the Randomness paragraph:

```markdown
**Annotation Privacy:** Annotation text (`[text]`) may contain personally identifiable information (PII) or sensitive context (e.g., character names, campaign details). Implementations that log, persist, or transmit roll results SHOULD treat annotation content as potentially sensitive and apply appropriate data handling policies.
```

- [ ] **Step 2: Verify build**

Run: `bun run --filter '@randsum/rdn' build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add apps/rdn/src/content/specs/v1.0-alpha.md
git commit -m "feat(spec): add annotation privacy note to Security Considerations"
```

---

### Task 4: Add Per-Level Test Vector Cross-Index

**Files:**
- Modify: `apps/rdn/src/content/specs/v1.0-alpha.md` — add to end of Appendix G

- [ ] **Step 1: Add cross-index table**

At the end of Appendix G (after G.10), add:

```markdown
### G.11 Conformance Level Cross-Index

Implementors can verify conformance at each level using the following vector subsets:

| Level | Required Vectors | Features Covered |
|-------|-----------------|-----------------|
| Level 1 (Core) | 1-3, 10-11, 14-16, 19-20, 26-27 | NdS, +, -, *, L, H, K, kl |
| Level 2 (VTT Baseline) | All Level 1 + 7, 9, 12-13, 18, 21-22, 25, 35, 40, 43, 45 | R, ro, !, !{}, #, S{}, F{}, conditions |
| Level 3 (Extended) | All Level 2 + 4-6, 8, 23, 31, 34, 36-37, 41, 44 | C, !!, !p, U, d{}, dF, KM, V{}, **, //, %, d% |
| Level 4 (Full) | All Level 3 + 32-33, 38-39 | gN, DDN, W, !s{}, zN |
| Error Cases | 28-30, 46-48 | Invalid notation rejection |
```

- [ ] **Step 2: Verify build**

Run: `bun run --filter '@randsum/rdn' build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add apps/rdn/src/content/specs/v1.0-alpha.md
git commit -m "feat(spec): add conformance level test vector cross-index (G.11)"
```

---

### Task 5: Add Status of This Memo

**Files:**
- Modify: `apps/rdn/src/content/specs/v1.0-alpha.md` — add after Abstract, before Section 1

- [ ] **Step 1: Add status section**

After the Abstract block and before `## 1. Introduction`, add:

```markdown
## Status of This Memo

This document is a community specification published by the RANDSUM project. It is not an Internet Standards Track specification and has not been submitted to the IETF. Distribution of this memo is unlimited.

This is an alpha release. The specification is believed to be stable for implementation but may undergo minor revisions before the 1.0 release. Feedback is welcome via the project repository.
```

- [ ] **Step 2: Verify build**

Run: `bun run --filter '@randsum/rdn' build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add apps/rdn/src/content/specs/v1.0-alpha.md
git commit -m "feat(spec): add Status of This Memo section"
```

---

### Task 6: Verify All Categories at 5/5

This is a verification-only task — no code changes.

- [ ] **Step 1: Review all categories**

Confirm each IETF category is now addressed:

| Category | Requirement | How Met |
|----------|------------|---------|
| Document Structure | Abstract, Status, Authors, History, ToC | Abstract (T1 prior), Status (T5), Authors (T1), History (T2). ToC auto-generated by Astro. |
| Normative Language | BCP 14 boilerplate, consistent MUST/SHOULD/MAY | Done in prior session |
| Security Considerations | DoS, injection, XSS, PRNG, privacy | DoS+injection+XSS (prior), PRNG (prior), privacy (T3) |
| IANA Considerations | Present even if empty | Done in prior session |
| References | Normative + informative, all citations resolved | BCP14, RFC5234, RFC7405, RFC3629 + informative platforms (prior) |
| Formal Grammar | Complete ABNF, disambiguation constraints | Appendix F (prior), contradictions fixed (prior) |
| Test Vectors | All modifiers, error cases, level cross-index | 48 vectors (prior) + cross-index (T4) |
| Encoding & Lexical | Encoding, whitespace, segmentation, disambiguation | Sections 8.0, 8.2, 8.3 (prior), fixes (prior) |
| Conformance Levels | Clear levels, claim format, test mapping | Section 10 + G.11 cross-index (T4) |
| Technical Completeness | All modifiers, pipeline, composition | Comprehensive throughout |

- [ ] **Step 2: Final build verification**

Run: `bun run --filter '@randsum/rdn' build`
Expected: PASS

---

## Summary

| Task | Effort | Category Improved |
|------|--------|-------------------|
| T1: Authors' Addresses | Small | Document Structure |
| T2: Document History | Small | Document Structure |
| T3: Annotation Privacy | Small | Security Considerations |
| T4: Level Cross-Index | Small | Conformance, Test Vectors |
| T5: Status of Memo | Small | Document Structure |
| T6: Verify 5/5 | Verify only | All |
